# 前端界面设计详细说明

## 1. 界面层次结构

### 1.1 应用整体结构
```
Electron 应用
├── 登录界面 (Login)
├── 管理者主界面 (AdminDashboard)
│   ├── 项目管理 (ProjectManagement)
│   ├── 员工管理 (EmployeeManagement)
│   └── 系统设置 (SystemSettings)
├── 员工工作界面 (EmployeeDashboard)
│   ├── 个人工作台 (PersonalWorkspace)
│   └── 项目访问 (ProjectAccess)
└── 项目详情界面 (ProjectDetail)
    ├── 总览 (Overview)
    ├── 看板 (Dashboard)
    ├── 项目进度 (Progress)
    ├── 里程碑 (Milestones)
    └── 五阶段模块
        ├── 研阶段 (ResearchPhase)
        ├── 建阶段 (BuildPhase)
        ├── 用阶段 (UsePhase) - 独立应用
        ├── 管阶段 (ManagePhase)
        └── 维阶段 (MaintainPhase)
```

## 2. 详细界面设计

### 2.1 登录界面 (Login.vue)

**布局设计**：
- 居中的登录卡片，背景为系统主题色渐变
- 左侧显示系统Logo和标题
- 右侧为登录表单区域

**界面元素**：
```vue
<template>
  <div class="login-container">
    <div class="login-card">
      <div class="logo-section">
        <img src="@/assets/logo.png" alt="系统Logo">
        <h1>研建用管维演示验证系统</h1>
        <p>v1.0.0</p>
      </div>
      <div class="form-section">
        <el-form :model="loginForm" :rules="rules">
          <el-form-item prop="username">
            <el-input v-model="loginForm.username" placeholder="用户名">
              <template #prefix>
                <el-icon><User /></el-icon>
              </template>
            </el-input>
          </el-form-item>
          <el-form-item prop="password">
            <el-input v-model="loginForm.password" type="password" placeholder="密码">
              <template #prefix>
                <el-icon><Lock /></el-icon>
              </template>
            </el-input>
          </el-form-item>
          <el-form-item prop="role">
            <el-select v-model="loginForm.role" placeholder="选择角色">
              <el-option label="管理者" value="admin"></el-option>
              <el-option label="研发人员" value="research"></el-option>
              <el-option label="建设人员" value="build"></el-option>
              <el-option label="使用人员" value="use"></el-option>
              <el-option label="管理人员" value="manage"></el-option>
              <el-option label="维护人员" value="maintain"></el-option>
            </el-select>
          </el-form-item>
          <el-form-item>
            <el-button type="primary" @click="handleLogin" :loading="loading">
              登录系统
            </el-button>
          </el-form-item>
        </el-form>
      </div>
    </div>
  </div>
</template>
```

**功能特性**：
- 表单验证（用户名、密码必填，角色必选）
- 记住登录状态
- 错误提示和加载状态
- 响应式设计，适配不同屏幕尺寸

### 2.2 管理者主界面 (AdminDashboard.vue)

**布局设计**：
- 顶部导航栏：系统标题、用户信息、退出按钮
- 左侧菜单栏：项目管理、员工管理、系统设置
- 主内容区域：根据菜单切换显示不同模块

**界面结构**：
```vue
<template>
  <div class="admin-layout">
    <el-container>
      <el-header class="header">
        <div class="header-left">
          <h2>研建用管维管理系统</h2>
        </div>
        <div class="header-right">
          <el-dropdown @command="handleCommand">
            <span class="user-info">
              <el-avatar :size="32" :src="userAvatar"></el-avatar>
              <span>{{ userName }}</span>
            </span>
            <template #dropdown>
              <el-dropdown-menu>
                <el-dropdown-item command="profile">个人设置</el-dropdown-item>
                <el-dropdown-item command="logout">退出登录</el-dropdown-item>
              </el-dropdown-menu>
            </template>
          </el-dropdown>
        </div>
      </el-header>
      
      <el-container>
        <el-aside width="200px" class="sidebar">
          <el-menu :default-active="activeMenu" @select="handleMenuSelect">
            <el-menu-item index="projects">
              <el-icon><Folder /></el-icon>
              <span>项目管理</span>
            </el-menu-item>
            <el-menu-item index="employees">
              <el-icon><User /></el-icon>
              <span>员工管理</span>
            </el-menu-item>
            <el-menu-item index="settings">
              <el-icon><Setting /></el-icon>
              <span>系统设置</span>
            </el-menu-item>
          </el-menu>
        </el-aside>
        
        <el-main class="main-content">
          <component :is="currentComponent" />
        </el-main>
      </el-container>
    </el-container>
  </div>
</template>
```

#### 2.2.1 项目管理模块 (ProjectManagement.vue)

**功能区域**：
1. **操作工具栏**：
   - 新建项目按钮
   - 搜索框（按项目名称搜索）
   - 状态筛选器（全部/进行中/已完成/暂停）
   - 视图切换（卡片视图/列表视图）

2. **项目列表区域**：
   - 项目卡片展示（默认视图）
   - 项目信息：名称、状态、进度、预算使用情况
   - 操作按钮：查看详情、下载里程碑、暂停/继续、删除

**项目卡片设计**：
```vue
<template>
  <el-card class="project-card" shadow="hover">
    <template #header>
      <div class="card-header">
        <span class="project-name">{{ project.name }}</span>
        <el-tag :type="getStatusType(project.status)">
          {{ getStatusText(project.status) }}
        </el-tag>
      </div>
    </template>
    
    <div class="project-info">
      <div class="info-row">
        <span class="label">当前阶段：</span>
        <span class="value">{{ getPhaseText(project.currentPhase) }}</span>
      </div>
      <div class="info-row">
        <span class="label">项目进度：</span>
        <el-progress :percentage="project.progress" />
      </div>
      <div class="info-row">
        <span class="label">预算使用：</span>
        <span class="value">
          {{ project.usedBudget }} / {{ project.totalBudget }}
          ({{ ((project.usedBudget / project.totalBudget) * 100).toFixed(1) }}%)
        </span>
      </div>
      <div class="info-row">
        <span class="label">团队人数：</span>
        <span class="value">{{ project.teamSize }}人</span>
      </div>
    </div>
    
    <template #footer>
      <div class="card-actions">
        <el-button size="small" @click="viewProject(project.id)">
          查看详情
        </el-button>
        <el-button size="small" @click="downloadMilestones(project.id)">
          下载里程碑
        </el-button>
        <el-dropdown @command="handleAction">
          <el-button size="small">
            更多<el-icon><ArrowDown /></el-icon>
          </el-button>
          <template #dropdown>
            <el-dropdown-menu>
              <el-dropdown-item :command="`pause-${project.id}`">
                暂停项目
              </el-dropdown-item>
              <el-dropdown-item :command="`delete-${project.id}`">
                删除项目
              </el-dropdown-item>
            </el-dropdown-menu>
          </template>
        </el-dropdown>
      </div>
    </template>
  </el-card>
</template>
```

#### 2.2.2 新建项目对话框 (CreateProjectDialog.vue)

**步骤化创建流程**：
```vue
<template>
  <el-dialog v-model="visible" title="新建项目" width="800px">
    <el-steps :active="currentStep" finish-status="success">
      <el-step title="基本信息"></el-step>
      <el-step title="资源配置"></el-step>
      <el-step title="确认创建"></el-step>
    </el-steps>
    
    <div class="step-content">
      <!-- 步骤1：基本信息 -->
      <div v-show="currentStep === 0" class="step-panel">
        <el-form :model="projectForm" :rules="rules" label-width="120px">
          <el-form-item label="项目名称" prop="name">
            <el-input v-model="projectForm.name" placeholder="请输入项目名称" />
          </el-form-item>
          <el-form-item label="项目描述" prop="description">
            <el-input v-model="projectForm.description" type="textarea" 
                     placeholder="请输入项目描述" :rows="3" />
          </el-form-item>
          <el-form-item label="项目预算" prop="budget">
            <el-input-number v-model="projectForm.budget" 
                           :min="10000" :max="1000000" :step="1000" />
          </el-form-item>
        </el-form>
      </div>
      
      <!-- 步骤2：资源配置 -->
      <div v-show="currentStep === 1" class="step-panel">
        <div class="template-selection">
          <h4>选择资源模板</h4>
          <el-radio-group v-model="selectedTemplate">
            <el-radio-button v-for="template in templates" 
                           :key="template.id" :label="template.id">
              {{ template.name }}
            </el-radio-button>
          </el-radio-group>
        </div>
        
        <div class="team-config">
          <h4>团队配置</h4>
          <el-table :data="teamConfig" style="width: 100%">
            <el-table-column prop="role" label="角色" width="120" />
            <el-table-column prop="count" label="人数" width="100">
              <template #default="{ row }">
                <el-input-number v-model="row.count" :min="0" :max="10" size="small" />
              </template>
            </el-table-column>
            <el-table-column prop="dailySalary" label="日薪" width="120">
              <template #default="{ row }">
                {{ row.dailySalary }} 元/天
              </template>
            </el-table-column>
            <el-table-column prop="totalCost" label="预估成本">
              <template #default="{ row }">
                {{ (row.count * row.dailySalary * 30).toLocaleString() }} 元/月
              </template>
            </el-table-column>
          </el-table>
        </div>
        
        <div class="time-config">
          <h4>时间配置</h4>
          <el-form-item label="时间倍率">
            <el-slider v-model="timeMultiplier" :min="0.1" :max="3" :step="0.1" 
                      show-tooltip tooltip-class="time-tooltip" />
            <span class="multiplier-text">{{ timeMultiplier }}x</span>
          </el-form-item>
        </div>
      </div>
      
      <!-- 步骤3：确认创建 -->
      <div v-show="currentStep === 2" class="step-panel">
        <div class="project-summary">
          <h4>项目信息确认</h4>
          <el-descriptions :column="2" border>
            <el-descriptions-item label="项目名称">{{ projectForm.name }}</el-descriptions-item>
            <el-descriptions-item label="项目预算">{{ projectForm.budget.toLocaleString() }} 元</el-descriptions-item>
            <el-descriptions-item label="团队总人数">{{ totalTeamSize }} 人</el-descriptions-item>
            <el-descriptions-item label="预估月成本">{{ estimatedMonthlyCost.toLocaleString() }} 元</el-descriptions-item>
            <el-descriptions-item label="时间倍率">{{ timeMultiplier }}x</el-descriptions-item>
            <el-descriptions-item label="预估完成时间">{{ estimatedDuration }} 天</el-descriptions-item>
          </el-descriptions>
        </div>
      </div>
    </div>
    
    <template #footer>
      <div class="dialog-footer">
        <el-button @click="visible = false">取消</el-button>
        <el-button v-if="currentStep > 0" @click="prevStep">上一步</el-button>
        <el-button v-if="currentStep < 2" type="primary" @click="nextStep">下一步</el-button>
        <el-button v-if="currentStep === 2" type="primary" @click="createProject" 
                  :loading="creating">创建项目</el-button>
      </div>
    </template>
  </el-dialog>
</template>
```

#### 2.2.3 员工管理模块 (EmployeeManagement.vue)

**界面布局**：
- 顶部工具栏：添加员工、批量操作、搜索筛选
- 员工列表表格：显示员工基本信息、当前状态、绩效评分
- 右侧详情面板：显示选中员工的详细信息和任务历史

**员工列表表格**：
```vue
<template>
  <div class="employee-management">
    <div class="toolbar">
      <el-button type="primary" @click="showAddDialog">添加员工</el-button>
      <el-input v-model="searchText" placeholder="搜索员工姓名" 
               style="width: 200px; margin-left: 10px;">
        <template #prefix>
          <el-icon><Search /></el-icon>
        </template>
      </el-input>
      <el-select v-model="statusFilter" placeholder="状态筛选" 
                style="width: 120px; margin-left: 10px;">
        <el-option label="全部" value=""></el-option>
        <el-option label="工作中" value="working"></el-option>
        <el-option label="空闲" value="idle"></el-option>
        <el-option label="离线" value="offline"></el-option>
      </el-select>
    </div>
    
    <el-table :data="filteredEmployees" @selection-change="handleSelectionChange"
             @row-click="showEmployeeDetail">
      <el-table-column type="selection" width="55" />
      <el-table-column prop="avatar" label="头像" width="80">
        <template #default="{ row }">
          <el-avatar :size="40" :src="row.avatar">{{ row.name[0] }}</el-avatar>
        </template>
      </el-table-column>
      <el-table-column prop="name" label="姓名" width="120" />
      <el-table-column prop="role" label="角色" width="100">
        <template #default="{ row }">
          <el-tag :type="getRoleType(row.role)">{{ getRoleText(row.role) }}</el-tag>
        </template>
      </el-table-column>
      <el-table-column prop="status" label="状态" width="100">
        <template #default="{ row }">
          <el-tag :type="getStatusType(row.status)">{{ getStatusText(row.status) }}</el-tag>
        </template>
      </el-table-column>
      <el-table-column prop="currentProject" label="当前项目" width="150" />
      <el-table-column prop="totalScore" label="总积分" width="100" />
      <el-table-column prop="efficiency" label="工作效率" width="100">
        <template #default="{ row }">
          <el-rate v-model="row.efficiency" :max="5" disabled show-score />
        </template>
      </el-table-column>
      <el-table-column prop="dailySalary" label="日薪" width="100">
        <template #default="{ row }">
          {{ row.dailySalary }} 元
        </template>
      </el-table-column>
      <el-table-column label="操作" width="150">
        <template #default="{ row }">
          <el-button size="small" @click.stop="editEmployee(row)">编辑</el-button>
          <el-button size="small" type="danger" @click.stop="deleteEmployee(row)">删除</el-button>
        </template>
      </el-table-column>
    </el-table>
  </div>
</template>
```

### 2.3 员工工作界面 (EmployeeDashboard.vue)

**界面特点**：
- 简化的界面设计，专注于个人工作内容
- 个人工作台显示当前任务和待办事项
- 只能访问分配给自己的项目

**个人工作台布局**：
```vue
<template>
  <div class="employee-dashboard">
    <div class="welcome-section">
      <div class="user-info">
        <el-avatar :size="60" :src="userInfo.avatar">{{ userInfo.name[0] }}</el-avatar>
        <div class="user-details">
          <h3>{{ userInfo.name }}</h3>
          <p>{{ getRoleText(userInfo.role) }}</p>
          <el-tag :type="getStatusType(userInfo.status)">{{ getStatusText(userInfo.status) }}</el-tag>
        </div>
      </div>
      <div class="stats-cards">
        <el-card class="stat-card">
          <div class="stat-content">
            <div class="stat-number">{{ personalStats.totalTasks }}</div>
            <div class="stat-label">总任务数</div>
          </div>
        </el-card>
        <el-card class="stat-card">
          <div class="stat-content">
            <div class="stat-number">{{ personalStats.completedTasks }}</div>
            <div class="stat-label">已完成</div>
          </div>
        </el-card>
        <el-card class="stat-card">
          <div class="stat-content">
            <div class="stat-number">{{ personalStats.totalScore }}</div>
            <div class="stat-label">总积分</div>
          </div>
        </el-card>
      </div>
    </div>
    
    <div class="work-content">
      <div class="current-tasks">
        <h4>当前任务</h4>
        <el-timeline>
          <el-timeline-item v-for="task in currentTasks" :key="task.id"
                          :type="getTaskType(task.status)" :hollow="task.status !== 'active'">
            <el-card class="task-card">
              <div class="task-header">
                <span class="task-name">{{ task.name }}</span>
                <el-tag size="small" :type="getTaskStatusType(task.status)">
                  {{ getTaskStatusText(task.status) }}
                </el-tag>
              </div>
              <div class="task-details">
                <p>项目：{{ task.projectName }}</p>
                <p>阶段：{{ getPhaseText(task.phase) }}</p>
                <div class="task-progress">
                  <el-progress :percentage="task.progress" />
                </div>
              </div>
              <div class="task-actions" v-if="task.status === 'active'">
                <el-button size="small" @click="viewTaskDetail(task)">查看详情</el-button>
                <el-button size="small" type="primary" @click="completeTask(task)">完成任务</el-button>
              </div>
            </el-card>
          </el-timeline-item>
        </el-timeline>
      </div>
      
      <div class="project-access">
        <h4>我的项目</h4>
        <div class="project-grid">
          <el-card v-for="project in assignedProjects" :key="project.id" 
                  class="project-card" @click="enterProject(project.id)">
            <div class="project-info">
              <h5>{{ project.name }}</h5>
              <p>{{ getPhaseText(project.currentPhase) }}</p>
              <el-progress :percentage="project.progress" />
            </div>
          </el-card>
        </div>
      </div>
    </div>
  </div>
</template>
```

### 2.4 项目详情界面 (ProjectDetail.vue)

**主界面布局**：
- 顶部面包屑导航和项目基本信息
- 标签页切换：总览、看板、项目进度、里程碑
- 各标签页对应不同的子组件

```vue
<template>
  <div class="project-detail">
    <div class="project-header">
      <el-breadcrumb separator="/">
        <el-breadcrumb-item :to="{ path: '/admin' }">管理首页</el-breadcrumb-item>
        <el-breadcrumb-item>{{ projectInfo.name }}</el-breadcrumb-item>
      </el-breadcrumb>
      
      <div class="project-info-bar">
        <div class="project-title">
          <h2>{{ projectInfo.name }}</h2>
          <el-tag :type="getStatusType(projectInfo.status)">
            {{ getStatusText(projectInfo.status) }}
          </el-tag>
        </div>
        <div class="project-actions">
          <el-button @click="pauseProject" v-if="projectInfo.status === 'running'">
            暂停项目
          </el-button>
          <el-button @click="resumeProject" v-if="projectInfo.status === 'paused'">
            继续项目
          </el-button>
          <el-button @click="exportProject">导出项目</el-button>
        </div>
      </div>
    </div>
    
    <div class="project-content">
      <el-tabs v-model="activeTab" @tab-click="handleTabClick">
        <el-tab-pane label="总览" name="overview">
          <ProjectOverview :project-id="projectId" />
        </el-tab-pane>
        <el-tab-pane label="看板" name="dashboard">
          <ProjectDashboard :project-id="projectId" />
        </el-tab-pane>
        <el-tab-pane label="项目进度" name="progress">
          <ProjectProgress :project-id="projectId" />
        </el-tab-pane>
        <el-tab-pane label="里程碑" name="milestones">
          <ProjectMilestones :project-id="projectId" />
        </el-tab-pane>
      </el-tabs>
    </div>
  </div>
</template>
```

#### 2.4.1 总览标签页 (ProjectOverview.vue)

**布局设计**：
- 四象限布局：项目信息、资源分配、核心指标、团队状态

```vue
<template>
  <div class="project-overview">
    <el-row :gutter="20">
      <el-col :span="12">
        <el-card title="项目信息" class="info-card">
          <el-descriptions :column="1" border>
            <el-descriptions-item label="项目名称">{{ projectInfo.name }}</el-descriptions-item>
            <el-descriptions-item label="当前阶段">{{ getPhaseText(projectInfo.currentPhase) }}</el-descriptions-item>
            <el-descriptions-item label="项目进度">
              <el-progress :percentage="projectInfo.progress" />
            </el-descriptions-item>
            <el-descriptions-item label="预算使用">
              {{ projectInfo.usedBudget.toLocaleString() }} / {{ projectInfo.totalBudget.toLocaleString() }}
              ({{ budgetUsagePercentage }}%)
            </el-descriptions-item>
            <el-descriptions-item label="开始时间">{{ formatDate(projectInfo.startTime) }}</el-descriptions-item>
            <el-descriptions-item label="预计完成">{{ formatDate(projectInfo.estimatedEndTime) }}</el-descriptions-item>
          </el-descriptions>
        </el-card>
      </el-col>
      
      <el-col :span="12">
        <el-card title="资源分配" class="resource-card">
          <div ref="resourceChart" style="height: 300px;"></div>
        </el-card>
      </el-col>
    </el-row>
    
    <el-row :gutter="20" style="margin-top: 20px;">
      <el-col :span="12">
        <el-card title="核心指标" class="metrics-card">
          <div class="metrics-grid">
            <div v-for="metric in coreMetrics" :key="metric.name" class="metric-item">
              <div class="metric-value">{{ metric.value }}</div>
              <div class="metric-name">{{ metric.name }}</div>
              <div class="metric-trend" :class="metric.trend">
                <el-icon v-if="metric.trend === 'up'"><ArrowUp /></el-icon>
                <el-icon v-if="metric.trend === 'down'"><ArrowDown /></el-icon>
                {{ metric.change }}
              </div>
            </div>
          </div>
        </el-card>
      </el-col>
      
      <el-col :span="12">
        <el-card title="团队状态" class="team-card">
          <div class="team-members">
            <div v-for="member in teamMembers" :key="member.id" class="member-item">
              <el-avatar :size="40" :src="member.avatar">{{ member.name[0] }}</el-avatar>
              <div class="member-info">
                <div class="member-name">{{ member.name }}</div>
                <div class="member-role">{{ getRoleText(member.role) }}</div>
                <div class="member-status" :class="member.status">
                  {{ getStatusText(member.status) }}
                </div>
              </div>
              <div class="member-score">{{ member.score }}分</div>
            </div>
          </div>
        </el-card>
      </el-col>
    </el-row>
  </div>
</template>
```

#### 2.4.2 看板标签页 (ProjectDashboard.vue)

**功能设计**：
- 40个效能指标的分类展示
- 实时数据更新
- 指标异常告警

```vue
<template>
  <div class="project-dashboard">
    <div class="dashboard-header">
      <div class="filter-controls">
        <el-select v-model="selectedCategory" placeholder="指标分类">
          <el-option label="全部指标" value="all"></el-option>
          <el-option label="质量指标" value="quality"></el-option>
          <el-option label="进度指标" value="progress"></el-option>
          <el-option label="成本指标" value="cost"></el-option>
          <el-option label="风险指标" value="risk"></el-option>
        </el-select>
        <el-button @click="refreshMetrics" :loading="refreshing">刷新数据</el-button>
      </div>
      
      <div class="alert-summary">
        <el-alert v-if="alertCount > 0" :title="`当前有 ${alertCount} 个指标异常`" 
                 type="warning" show-icon />
      </div>
    </div>
    
    <div class="metrics-container">
      <div class="metrics-grid">
        <el-card v-for="metric in filteredMetrics" :key="metric.id" 
                class="metric-card" :class="{ 'alert': metric.isAlert }">
          <div class="metric-header">
            <span class="metric-name">{{ metric.name }}</span>
            <el-tag v-if="metric.isAlert" type="danger" size="small">异常</el-tag>
          </div>
          
          <div class="metric-content">
            <div class="metric-value-display">
              <span class="metric-value">{{ metric.value }}</span>
              <span class="metric-unit">{{ metric.unit }}</span>
            </div>
            
            <div class="metric-chart" :ref="`chart_${metric.id}`" style="height: 100px;"></div>
          </div>
          
          <div class="metric-footer">
            <div class="metric-trend">
              <span class="trend-text" :class="metric.trend">
                <el-icon v-if="metric.trend === 'up'"><TrendCharts /></el-icon>
                <el-icon v-if="metric.trend === 'down'"><TrendCharts /></el-icon>
                {{ metric.changePercent }}%
              </span>
            </div>
            <div class="metric-time">{{ formatTime(metric.updateTime) }}</div>
          </div>
        </el-card>
      </div>
    </div>
  </div>
</template>
```

#### 2.4.3 项目进度标签页 (ProjectProgress.vue)

**三栏布局设计**：
- 左侧：基于研建用管维角色交互的XML时序图可视化
- 右上：当前任务执行员工信息和状态
- 右下：任务进度和细化事件列表

**XML时序图设计逻辑**：
- 基于《研建用管维角色关系与效能指标体系分析.md》中的17个关键任务时序图
- 使用D3.js或SVG渲染五角色（研、建、用、管、维）的交互流程
- 显示任务执行顺序：需求分析→系统设计→部署应用→维护优化→持续改进
- 支持点击任务节点查看详细信息，右侧同步更新任务详情
- 颜色状态标识：未完成（灰色）、进行中（蓝色）、已完成（黑色）

**手动/自动执行模式控制**：
- 提供三种执行模式选择：手动模式、自动模式、混合模式
- 手动模式：所有任务进展需要用户手动点击触发
- 自动模式：系统自动填充任务数据和进度，但需要用户确认后才正式更新
- 混合模式：自动执行基础流程，允许用户随时手动干预和调整
- 自动模式下显示数据预览对话框，用户可选择确认、修改或重新生成

```vue
<template>
  <div class="project-progress">
    <el-row :gutter="20" style="height: 100%;">
      <el-col :span="12" class="timeline-section">
        <el-card title="项目时序图" style="height: 100%;">
          <div class="timeline-controls">
            <el-button-group>
              <el-button @click="playTimeline" :disabled="isPlaying">播放</el-button>
              <el-button @click="pauseTimeline" :disabled="!isPlaying">暂停</el-button>
              <el-button @click="resetTimeline">重置</el-button>
            </el-button-group>
            <el-slider v-model="timelineSpeed" :min="0.5" :max="3" :step="0.5" 
                      style="width: 200px; margin-left: 20px;" />
          </div>
          
          <div class="timeline-container" ref="timelineContainer">
            <!-- 基于研建用管维的XML时序图可视化 -->
            <div class="phases-timeline">
              <div v-for="phase in phases" :key="phase.id" 
                  class="phase-block" :class="{ 
                    'completed': phase.status === 'completed',
                    'active': phase.status === 'active',
                    'pending': phase.status === 'pending'
                  }">
                <div class="phase-header">
                  <h4>{{ phase.name }}</h4>
                  <el-progress :percentage="phase.progress" :show-text="false" />
                </div>
                
                <div class="tasks-list">
                  <div v-for="task in phase.tasks" :key="task.id"
                      class="task-item" :class="{
                        'completed': task.status === 'completed',
                        'active': task.status === 'active',
                        'pending': task.status === 'pending'
                      }"
                      @click="selectTask(task)">
                    <div class="task-dot"></div>
                    <span class="task-name">{{ task.name }}</span>
                    <span class="task-duration">{{ task.duration }}天</span>
                    <span class="task-role">{{ getRoleText(task.primaryRole) }}</span>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </el-card>
      </el-col>
      
      <el-col :span="12">
        <el-row :gutter="20" style="height: 50%;">
          <el-col :span="24">
            <el-card title="当前任务执行人" class="current-task-card">
              <div v-if="currentTask" class="task-executor">
                <div class="executor-info">
                  <el-avatar :size="60" :src="currentTask.executor.avatar">
                    {{ currentTask.executor.name[0] }}
                  </el-avatar>
                  <div class="executor-details">
                    <h4>{{ currentTask.executor.name }}</h4>
                    <p>{{ getRoleText(currentTask.executor.role) }}</p>
                    <el-tag :type="getStatusType(currentTask.executor.status)">
                      {{ getStatusText(currentTask.executor.status) }}
                    </el-tag>
                  </div>
                </div>
                
                <div class="task-info">
                  <h5>{{ currentTask.name }}</h5>
                  <p>{{ currentTask.description }}</p>
                  <div class="task-progress">
                    <el-progress :percentage="currentTask.progress" />
                    <span class="progress-text">{{ currentTask.progress }}%</span>
                  </div>
                </div>
              </div>
              
              <div v-else class="no-current-task">
                <el-empty description="当前没有执行中的任务" />
              </div>
            </el-card>
          </el-col>
        </el-row>
        
        <el-row :gutter="20" style="height: 50%; margin-top: 20px;">
          <el-col :span="24">
            <el-card title="任务事件日志" class="events-card">
                <div class="events-container">
                  <div class="event-controls">
                    <el-radio-group v-model="executionMode" @change="handleModeChange">
                      <el-radio-button label="manual">手动模式</el-radio-button>
                      <el-radio-button label="automatic">自动模式</el-radio-button>
                      <el-radio-button label="mixed">混合模式</el-radio-button>
                    </el-radio-group>
                    <el-button v-if="executionMode !== 'automatic'" 
                              type="primary" size="small" 
                              @click="executeCurrentTask" :loading="executing">
                      执行当前任务
                    </el-button>
                  </div>
                  
                  <el-timeline>
                    <el-timeline-item v-for="event in taskEvents" :key="event.id"
                                    :timestamp="formatTime(event.timestamp)"
                                    :type="getEventType(event.type)">
                      <div class="event-content">
                        <div class="event-title">{{ event.title }}</div>
                        <div class="event-description">{{ event.description }}</div>
                        <div class="event-impact" v-if="event.impact">
                          <el-tag size="small" :type="getImpactType(event.impact.type)">
                            {{ event.impact.description }}
                          </el-tag>
                        </div>
                      </div>
                    </el-timeline-item>
                  </el-timeline>
                </div>
            </el-card>
          </el-col>
        </el-row>
      </el-col>
    </el-row>
  </div>
</template>
```

#### 2.4.4 里程碑标签页 (ProjectMilestones.vue)

**功能设计**：
- 里程碑列表展示
- 文档下载功能
- 里程碑详情查看

```vue
<template>
  <div class="project-milestones">
    <div class="milestones-header">
      <h3>项目里程碑</h3>
      <el-button @click="downloadAllMilestones" type="primary">
        下载全部里程碑
      </el-button>
    </div>
    
    <div class="milestones-timeline">
      <el-timeline>
        <el-timeline-item v-for="milestone in milestones" :key="milestone.id"
                        :timestamp="formatDate(milestone.createdAt)"
                        :type="getMilestoneType(milestone.status)">
          <el-card class="milestone-card">
            <div class="milestone-header">
              <div class="milestone-info">
                <h4>{{ milestone.name }}</h4>
                <p>{{ milestone.phase }} - {{ milestone.description }}</p>
              </div>
              <div class="milestone-actions">
                <el-button size="small" @click="viewMilestone(milestone)">
                  查看详情
                </el-button>
                <el-button size="small" @click="downloadMilestone(milestone)">
                  下载文档
                </el-button>
              </div>
            </div>
            
            <div class="milestone-content">
              <div class="deliverables">
                <h5>交付物：</h5>
                <el-tag v-for="deliverable in milestone.deliverables" 
                       :key="deliverable" size="small" style="margin-right: 8px;">
                  {{ deliverable }}
                </el-tag>
              </div>
              
              <div class="milestone-metrics" v-if="milestone.metrics">
                <h5>关键指标：</h5>
                <div class="metrics-summary">
                  <span v-for="metric in milestone.metrics" :key="metric.name"
                       class="metric-item">
                    {{ metric.name }}: {{ metric.value }}{{ metric.unit }}
                  </span>
                </div>
              </div>
            </div>
          </el-card>
        </el-timeline-item>
      </el-timeline>
    </div>
  </div>
</template>
```

## 3. 五阶段功能模块详细设计

**设计原则**：
- 基于《研建用管维角色关系与效能指标体系分析.md》中的17个关键任务流程
- 每个阶段都包含详细的任务事件模板，支持自定义事件配置
- 集成意外事件模拟机制，包括需求变更、Bug发现等场景
- 支持手动/自动执行模式，提供灵活的项目控制方式

### 3.1 研阶段模块 (ResearchPhase.vue)

**功能设计逻辑**：
- **需求统一受理机制**：提供SRS文档上传界面，支持Word、PDF格式自动解析
- **大模型智能评价**：集成OpenAI等大模型API，对需求进行五维度评估
- **需求管理面板**：支持需求导入、分解、跟踪和变更管理
- **多方案对比工具**：提供参数化权衡模型，支持技术方案对比分析

**任务事件模板**：
- 需求分析任务包含：开始需求分析→需求文档撰写→需求评审准备→需求评审会议→需求修订
- 每个事件都有明确的持续时间和对效能指标的影响
- 支持手动触发和自动执行两种模式

**大模型评价逻辑**：
- 评分维度：清晰度、完整性、一致性、可行性、可测试性（各0-100分）
- 自动生成问题清单和改进建议
- 提供评估标准配置和结果可视化展示
- 支持评估结果导出和历史对比

### 3.2 用阶段模块（独立应用）

**特殊设计逻辑**：
- 用阶段作为独立的前端应用，纯前端实现，无需后端支持
- 模拟真实的监控系统使用场景，展示软件价值体现过程
- 当项目进度到达软件部署步骤时，提供链接打开此独立应用

**功能实现逻辑**：
- **实时数据监控**：模拟传感器数据生成（温度、湿度、压力、流量等）
- **阈值告警机制**：数据超过预设阈值时自动触发告警，发送到维护阶段
- **用户操作模拟**：模拟管理员日常巡检、参数调整、异常处理等操作
- **性能指标展示**：实时显示系统可用率、响应时间、用户满意度等指标

**意外事件触发**：
- 模拟使用过程中发现严重Bug，自动触发维护流程
- 支持手动触发各种异常场景，演示系统应对能力
- 用户反馈收集功能，为需求迭代提供输入

## 4. 响应式设计和用户体验

### 4.1 响应式布局
- 使用Element Plus的栅格系统
- 支持不同屏幕尺寸的适配
- 移动端友好的界面设计

### 4.2 交互体验优化
- 加载状态指示器
- 操作反馈提示
- 快捷键支持
- 拖拽排序功能

### 4.3 主题和样式
- 统一的设计语言
- 可切换的主题模式（明亮/暗黑）
- 品牌色彩方案
- 图标系统

## 5. 性能优化

### 5.1 前端性能优化
- 组件懒加载
- 虚拟滚动
- 图片懒加载
- 缓存策略

### 5.2 用户体验优化
- 骨架屏加载
- 渐进式加载
- 离线缓存
- 错误边界处理

这份详细的前端界面设计说明涵盖了所有主要界面的结构、功能和实现方式，为开发提供了完整的指导。
