请按照需求，检查技术方案，这只是一个前端后端的应用，不需要使用微服务。
研阶段，有需求统一受理机制，具体实现方式是，用户上传SRS，格式化处理，创建任务；
利用大模型，设置一套评判标注，为需求打分；
管理阶段，设置不同资源模板，模型不同资源下，项目开发进度；通过设置函数，影响各阶段任务的时间倍率；
每个阶段结束，是里程碑，并输出每个阶段成果，存储到数据库中；
从内容中再总结出几条机制实现，补充到10条；
外部接口，主要就是研的阶段，支持上传SRS文档创建新的项目任务；维护阶段，支持上传监控系统实时状态数据，触发不同的处理流程；

请根据甲方需求，调整技术方案中其他内容。
